# Python Layer

This folder is reserved for Python strategy and research interfaces.

Keep production C++ backtesting code in `core/`, Win32 presentation code in `ui/`, and batch data workflows in `scripts/`.
